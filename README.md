# gym&coach — 健身与中台运营经验蒸馏

把你在头部健身公司 **8 年**（加盟 / 门店 / 教练 / 产品 / 数据）及 **互联网中台运营** 中的行业判断，沉淀为 Cursor Agent Skill。

**仓库**：https://github.com/Etude0711/gym-coach-distill

## 当前 Skill（主）

```
.cursor/skills/gym-coach-distill/
├── SKILL.md          # Agent 主文件
├── INTERVIEW.md      # 全量访谈题纲（每模块 8–15 题）
├── PRIORITY.md       # 必须先答的 8 题
├── README.md         # Skill 使用说明
├── examples.md       # 试跑样例（待填）
└── references/       # 判断库（ANSWERS 蒸馏后写入）
```

**进度**：PRIORITY 已答；skill **固定在本仓库** `.cursor/skills/gym-coach-distill/`。  
**进度**：PRIORITY（P1–P8）+ P4/P5/P3/P2 已全部落盘；自查见 `self-check.md`。  
**可选**：继续 INTERVIEW 模块，或用真实问题试跑 `@gym-coach-distill`。

## 旧版（可忽略）

`.cursor/skills/gym-ops-distill/` 为早期简版，**以 `gym-coach-distill` 为准**。

## 克隆后使用

```bash
git clone https://github.com/Etude0711/gym-coach-distill.git
cd gym-coach-distill
```

在 Cursor 打开该目录，使用 `@gym-coach-distill` 或说「自查健身房的经营」。

## 使用方式

- 在 Cursor 中 `@gym-coach-distill` 或说明「用健身运营蒸馏 skill」。
- 中文口令：门店诊断、加盟五问、**自查健身房**、回报率、门店运营数据 …

## 日志

后续若增加校验/蒸馏脚本，运行日志写入 `logs/`，文件名带时间戳（例：`distill-20260604-120000.log`）。
